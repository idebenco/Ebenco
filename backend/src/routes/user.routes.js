const express = require('express');
const router = express.Router();

// Placeholder for future user-specific routes
// Currently, user operations are handled through auth routes

module.exports = router;
